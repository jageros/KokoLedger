import SwiftUI

struct HomePlaceholderView: View {
    var body: some View {
        PlaceholderPageView(
            title: "首页 / 记账",
            systemImage: "square.and.pencil"
        )
    }
}

#Preview {
    HomePlaceholderView()
}
