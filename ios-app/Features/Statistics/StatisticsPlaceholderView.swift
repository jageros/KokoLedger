import SwiftUI

struct StatisticsPlaceholderView: View {
    var body: some View {
        PlaceholderPageView(
            title: "统计 / 分析",
            systemImage: "chart.line.uptrend.xyaxis"
        )
    }
}

#Preview {
    StatisticsPlaceholderView()
}
